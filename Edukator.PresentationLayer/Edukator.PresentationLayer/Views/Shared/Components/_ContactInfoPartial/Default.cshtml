@using Edukator.EntityLayer.Concrete
@model ContactInfo
<div class="contacts-5-grid-main mt-5">
                <div class="contacts-5-grid">
                    <div class="map-content-5">
                        <div class="d-grid grid-col-2">
                            <div class="contact-type">
                                <div class="address-grid">
                                    <h6><span class="fas fa-map-marked-alt"></span> Adres</h6>
                                    <p>@Model.Address1</p>
                                    <p>@Model.Address2</p>

                                </div>
                                <div class="address-grid">
                                    <h6><span class="fas fa-envelope-open-text"></span> Mail</h6>
                                    <a href="#" class="link1">@Model.Mail1</a>
                                    <a href="#" class="link1">@Model.Mail2</a>

                                </div>
                                <div class="address-grid">
                                    <h6><span class="fas fa-phone-alt"></span> Telefon</h6>
                                    <a href="tel:+12 324-016-695" class="link1">@Model.Phone1</a>
                                    <a href="tel:+44 224-058-545" class="link1">@Model.Phone2</a>

                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>